# Репетитор по немецкому A0–B2

Публичное HTML-приложение для самостоятельного изучения немецкого языка от полного новичка до B2.

## Быстрые входы

- **Я новичок** — старт с A0.
- **Проверить уровень** — диагностика.
- **Грамматика B2 коротко** — отдельный вход в главные темы B2.
- **Словарь отдельно** — фильтры по уровню, теме и поиску.

## Состав v14.2

- уровни A0 / A1 / A2 / B1 / B2;
- уроки: **78**;
- словарь и фразы: **464**;
- диагностические вопросы: **31**;
- экзаменационные промпты: **7**;
- прогресс через `localStorage`;
- кнопка `✓ изучено`;
- отдельный режим «скрывать изученное»;
- профильные темы: медицина, клиника, Швейцария, пластическая хирургия;
- мобильная навигация;
- печатный режим.

## Публичная ссылка

```text
https://n1njz74.github.io/deutsch-a0-b2-tutor/
```

## Версия

Текущая версия: **v15.4**.

V14.1 расширяет GitHub-версию до полного продукта по плану: больше уроков, больше словаря, больше диагностических вопросов, профильная лексика и отдельный журнал проверки.

## Проверка

Локальная визуальная проверка выполнена через Chromium/Playwright с `page.set_content`, потому что в этой среде прямые переходы `file://` и `localhost` блокировались политикой окружения. Сохранены скриншоты desktop/mobile в `docs/screenshots/` и метрики в `data/visual_check_metrics_v14_1.json`.


## v14.2

Изменения v14.2:

- добавлен адаптивный аудио-движок: авто, голос устройства, онлайн MP3, offline MP3 при наличии папки `audio/`;
- исправлено мобильное поведение озвучки: на телефоне авто-режим сначала пробует онлайн-MP3, затем голос устройства;
- исправлен баг раскрывающихся списков через `change`, а не общий `click`;
- уроков: 101;
- слов и фраз: 685;
- вопросов диагностики: 55;
- B2-промптов: 17;
- добавлены специализированные словари: медицина, клиника, Швейцария, пластическая хирургия, официальная переписка, строительство/АКЗ.


## Offline MP3 pack

В v14.2 добавлена папка `audio/` с 353 MP3-файлами для части уроков, стартовых фраз и профильной лексики. Если конкретной фразы нет в offline-пакете, авто-режим пробует online MP3 и голос устройства.

## v14.3

Изменения v14.3:

- online MP3 установлен как режим озвучки по умолчанию;
- auto-режим теперь пробует: online MP3 → голос устройства → offline MP3;
- добавлена диагностика ошибок online MP3 в поле «Последний движок»;
- добавлены no-cache meta-теги для снижения проблем со старым кэшем браузера.
## v14.4

Изменения v14.4:

- объединены правки v14.3 по мобильной озвучке и кэшу;
- online MP3 оставлен режимом по умолчанию;
- добавлена диагностика причин ошибки online MP3;
- после завершения проверки уровня добавлен полный разбор ответов:
  - выбранный вариант;
  - правильный вариант;
  - статус правильно/ошибка;
  - объяснение правила;
- объяснения скрыты до завершения диагностики.
## v14.5

Repair release:

- восстановлен рабочий `index.html` из последней стабильной v14.2-сборки;
- удалён сломанный внешний диагностический слой v14.4;
- разбор диагностики теперь показывается только на `#diagnostic` и только после результата;
- добавлены подробные объяснения причин правильного ответа;
- online MP3 оставлен режимом по умолчанию, добавлена диагностика ошибки;
- добавлены no-cache meta-теги и версия кэша.
## v14.6

Fix release:

- исправлена синтаксическая ошибка в `speak()`, из-за которой SPA не рендерился;
- убран внешний v14.5 diagnostic overlay;
- разбор диагностики встроен в штатный результат теста;
- после завершения теста показываются: выбранный ответ, правильный ответ, статус и объяснение;
- online MP3 остаётся режимом по умолчанию с fallback online → device → offline.
## v14.7

Изменения v14.7:

- режим Online MP3 снова использует старый online TTS через `translate_tts`;
- Online MP3 теперь строгий: если online заблокирован, он не подменяется голосом устройства;
- Auto-режим оставлен для fallback: online → device → offline;
- добавлены локальные профили пользователей с отдельным прогрессом;
- профильные словари medicine/swiss/surgery включены по умолчанию.
## v14.8

Fix release:

- доведены до commit/push изменения v14.7;
- исправлена проверка, которая ошибочно останавливала commit из-за нормального фрагмента `let used`;
- добавлены обработчики локальных профилей пользователей;
- Online MP3 оставлен строгим режимом: без скрытой подмены голосом устройства;
- Auto остаётся fallback-режимом: online → device → offline;
- профильные словари medicine/swiss/surgery включены по умолчанию.
## v14.9

Fix release:

- Online MP3 теперь пробует несколько online-TTS источников:
  - translate.googleapis.com client=gtx;
  - translate.google.com client=tw-ob;
  - translate.google.com client=gtx;
- режим Online MP3 остаётся строгим и не подменяется голосом устройства;
- Auto остаётся fallback-режимом: online → device → offline;
- сообщение `media 4` теперь показывает, какой online-источник упал.
## v15.0

Neural audio release:

- прямой online Google TTS признан нестабильным из-за `media 4`;
- плохой offline MP3-пакет заменён на neural MP3, сгенерированный через `edge-tts`;
- основной режим озвучки по умолчанию: Neural MP3 из папки `audio/`;
- Online MP3 оставлен только как экспериментальный запасной режим;
- Auto-режим теперь пробует: Neural MP3 → голос устройства → online;
- добавлен `data/audio_source_map_v15_0.json` для проверки соответствия файлов и немецких фраз.
## v15.1

Full neural audio release:

- MP3 теперь генерируются не только для старого manifest, а для всех немецких элементов приложения;
- покрываются словарь, уроки, примеры, упражнения, диагностика и B2-промпты;
- добавлен `data/audio_all_items_v15_1.json`;
- исправлена озвучка старых v13-словарных записей, где немецкая фраза могла лежать в `ru` или `example`;
- `audio/manifest.json` теперь отражает полный MP3-пакет.
## v15.2

Content audit and repair release:

- заменены все упоминания Украины на Россию в учебных фразах;
- исправлены старые v13-словарные записи, где немецкая фраза могла лежать в поле `ru`, а русский ярлык — в `de`;
- заполнены пустые объяснения в упражнениях и диагностике;
- исправлены отдельные механические примеры и согласование числа;
- добавлен отчёт `data/content_audit_v15_2.json`;
- обновлён полный neural-audio manifest;
- добавлена/обновлена фраза `Ich komme aus Russland.` и соответствующий MP3.
## v15.3

Lesson theory expansion:

- каждый урок получил расширенный блок «Подробно для русскоговорящего»;
- добавлены блоки «Русская ловушка», «Немецкая логика», «Формула»;
- добавлены грамматические таблицы внутри уроков;
- добавлены типичные ошибки;
- обновлён `renderLesson()`, чтобы показывать расширенную теорию;
- добавлен отчёт `data/lesson_theory_tables_v15_3.json`.

## v15.4

Lesson render repair:

- восстановлен отсутствующий `renderDrill()`;
- исправлено открытие уроков из `#path/A0` и прямого маршрута `#lesson/a0-01-ss`;
- упражнения снова привязаны к штатному обработчику `answer-drill`;
- добавлен отчёт `data/visual_diagnostic_report.json`;
- скриншоты визуальной диагностики сохранены в `docs/screenshots/visual_diagnostic/`;
- контрольные суммы пересобраны без самоссылки на `CHECKSUMS.sha256`.


## v15.5 Russian-speaker bridge

- Added dedicated A0-B2 bridge lessons for Russian-speaking learners.
- Expanded contrast examples: Russian transfer mistake -> correct German form.
- Rebuilt drill explanations so answers explain the actual grammar point.
- Added focused vocabulary topic `Русскоговорящий мост` and generated MP3 for new German items.
- Added diagnostic report: `data/tutor_diagnostic_v15_5.json` and `docs/diagnostics/tutor_diagnostic_v15_5.md`.


## v15.6 systematic quality

- Added level maps for A0, A1, A2, B1 and B2.
- Added visual schemes and mini quality graphs inside lessons.
- Expanded B2 with dedicated quality lessons: argumentation, Satzbau, Nominalstil/Passiv/zu-Infinitiv, register and error diagnosis.
- Added B2 vocabulary topic `B2 Qualität und Stil` and generated MP3 for new German material.
- Added diagnostic report: `data/systematic_quality_v15_6.json` and `docs/diagnostics/systematic_quality_v15_6.md`.
